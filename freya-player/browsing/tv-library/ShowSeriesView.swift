import SwiftUI

struct ShowSeriesView: View {
    let title: String

    var body: some View {
        FeatureStubView(
            title: title,
            message: "Series details will live here."
        )
    }
}
