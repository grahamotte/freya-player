import SwiftUI

struct TVLibraryIndexView: View {
    let title: String

    var body: some View {
        FeatureStubView(
            title: title,
            message: "Series browsing will live here."
        )
    }
}
