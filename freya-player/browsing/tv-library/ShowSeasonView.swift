import SwiftUI

struct ShowSeasonView: View {
    let title: String

    var body: some View {
        FeatureStubView(
            title: title,
            message: "Season browsing will live here."
        )
    }
}
