import SwiftUI

struct MovieLibraryIndexView: View {
    let title: String

    var body: some View {
        FeatureStubView(
            title: title,
            message: "Movie library browsing will live here."
        )
    }
}
