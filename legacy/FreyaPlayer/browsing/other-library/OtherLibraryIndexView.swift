import SwiftUI

struct OtherLibraryIndexView: View {
    let title: String

    var body: some View {
        FeatureStubView(
            title: title,
            message: "Browsing for other Plex library types will live here."
        )
    }
}
